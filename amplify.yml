version: 1
frontend:
  phases:
    preBuild:
      commands:
        - export NEXT_TELEMETRY_DISABLED=1
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: apps/web/out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - apps/web/.next/cache/**/*
