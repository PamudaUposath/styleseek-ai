version: 1
applications:
  - appRoot: apps/web
    backend:
      phases:
        build:
          commands:
            - npm ci --prefix ../..
            - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
    frontend:
      phases:
        preBuild:
          commands:
            - export NEXT_TELEMETRY_DISABLED=1
            - npm ci --prefix ../.. --no-audit --no-fund
        build:
          commands:
            - npm run build:shared --prefix ../..
            - npm run build:web --prefix ../..
      artifacts:
        baseDirectory: out
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
          - .next/cache/**/*
