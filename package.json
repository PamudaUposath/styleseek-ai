{
  "name": "styleseek-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "StyleSeek AI - AI-powered fashion discovery assistant built with Amazon Bedrock and Amazon Nova",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"API,WEB\" -c \"cyan,magenta\" \"npm run dev:api\" \"npm run dev:web\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run start:dev --workspace=apps/api",
    "build": "npm run build:shared && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:api": "npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "test": "npm run build:shared && npm run test --workspace=apps/api",
    "typecheck": "npm run build:shared && tsc -b tsconfig.base.json",
    "lint": "npx eslint apps/api/src apps/web/app apps/web/components apps/web/lib"
  },
  "keywords": [
    "amazon-bedrock",
    "amazon-nova",
    "fashion-ai",
    "nextjs",
    "nestjs",
    "aws-lambda",
    "api-gateway"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.5",
    "esbuild": "^0.28.2",
    "typescript": "^5.4.5"
  }
}
